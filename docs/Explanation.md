## Data Memory

The processor includes a simple RAM module used for data storage.

RAM operations:

STORE instruction  
Writes register data into RAM.

LOAD instruction  
Reads data from RAM into a register.

This allows the processor to interact with external data memory similar to real microprocessors.
